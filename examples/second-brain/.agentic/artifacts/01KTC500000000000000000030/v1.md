# Seed Reading Queue Snapshot

This artifact is the example's working queue. Treat it as mutable workspace state during a dogfood run.

## Status Legend

- `captured`: saved, but not yet worth agent time.
- `selected`: ready for synthesis because it supports an active question.
- `synthesized`: already converted into a durable artifact.
- `archive-candidate`: likely useful only as reference or should be discarded.

## Items

### How to Take Smart Notes

- Status: `selected`
- Why saved: Could clarify whether Zettelkasten-style notes are worth the overhead for a small personal system.
- Question: Which parts of the workflow help a lightweight second brain, and which parts are unnecessary ceremony?
- Decision supported: Choose whether the public example should model atomic permanent notes or simpler research briefs.

### The PARA Method

- Status: `captured`
- Why saved: Agentic examples already use PARA tags for artifact placement.
- Question: Does PARA give enough structure for queue triage without adding a separate taxonomy?
- Decision supported: Decide whether queue statuses should map directly to PARA buckets.

### 100 Productivity Hacks That Changed My Life

- Status: `archive-candidate`
- Why saved: Broad productivity advice might contain useful heuristics.
- Question: Is there a concrete second-brain decision this supports?
- Decision supported: Decide whether broad low-signal captures should be archived aggressively.

### Building a Reading Habit That Lasts

- Status: `captured`
- Why saved: Might help distinguish a reading queue from a habit tracker.
- Question: Should the queue optimize for reading cadence, research output, or both?
- Decision supported: Decide whether review cadence belongs in the queue artifact or a separate weekly-review workflow.

## Current Steward Note

Only one item should be synthesized in the first dogfood pass. The rest should be left for a steward review so the example demonstrates follow-up task creation instead of pretending one research run cleans the whole system.

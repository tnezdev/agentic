id: local-demo
kind: deploy_target
description: Local filesystem deployment for inspecting the bundle/runtime boundary.
state:
  adapter: filesystem
  dir: .agentic/.data
materializes:
  - surfaces
  - schedules
  - hooks
  - capabilities
not_included:
  - model_provider
  - approval_ui
  - real_review_queue
  - secrets
  - network_access
